// const verificar = confirm('Quer continuar?');
// if (verificar == true) {
//     const nome = prompt('Digite seu nome:');
//     cumprimentar(nome);
// }

// function cumprimentar(nome){
//     alert(`Olá ${nome}, seu nome foi salvo com sucesso!`);
// }

// Pegar dois números
const num1 = Number(prompt('Digite um número'));
const num2 = Number(prompt('Digite outro número'));

// Somar esses dois números
const resultado = num1+num2;

// Falar o resultado desse cálculo
alert(`O resultado desse cálculo é ${resultado}`);
