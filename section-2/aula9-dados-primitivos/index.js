const nome = 'Augusto'; // String
const nome1 = "Augusto";
const nome2 = `Augusto`;
const num1 = 10; //number
const num2 = 10.11; //number
let nomeAluno; // undefined=não aponta pra local nenhum na memória
const sobrenomeAluno = null; // Nulo>não ponta pra local nenhum na memória
const aprovado = true; // boolean = true, false (lógico) 

const a = [1,2];
const b = a;
console.log(a, b);

b.push(3);
console.log(a,b);

